package com.labas.store.dao;

import com.labas.store.model.entity.PaymentMethod;

/**
 * Interface for PaymentMethod-specific DAO operations.
 */
public interface PaymentMethodDAO extends GenericDAO<PaymentMethod, Long> {
    // Add additional methods specific to PaymentMethod if needed
}
